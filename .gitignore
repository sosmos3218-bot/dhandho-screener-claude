# 민감정보 — 절대 커밋 금지
secrets.json

# macOS
.DS_Store

# 가상환경 / 캐시 / 로그
.venv/
.cache/
logs/
__pycache__/
*.pyc

# 생성물 (원하면 추적 가능 — 기본은 제외)
newsletter/
# snapshots/ 는 시계열 추적용이라 커밋해도 됨 (필요 시 아래 주석 해제)
# snapshots/

# Cloudflare Worker(webhook/) 로컬 빌드/의존성
webhook/node_modules/
webhook/.wrangler/
